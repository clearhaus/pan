dependencies:
    override:
        - 'rvm-exec 2.1.9 bundle install'
        - 'rvm-exec 2.2.5 bundle install'
        - 'rvm-exec 2.3.1 bundle install'

test:
    override:
        - 'rvm-exec 2.1.9 bundle exec rspec'
        - 'rvm-exec 2.2.5 bundle exec rspec'
        - 'rvm-exec 2.3.1 bundle exec rspec'
